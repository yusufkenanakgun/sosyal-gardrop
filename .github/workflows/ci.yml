name: CI (Lint / Typecheck / Build)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  ci:
    runs-on: ubuntu-latest
    name: Lint • Typecheck • Build

    env:
      NEXT_TELEMETRY_DISABLED: "1"
      CI: "true"
      # Build-time'da gerçek bağlantı yapmasın diye dummy env'ler
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/sw_dev_dummy"
      REDIS_URL: "redis://localhost:6379"
      S3_ENDPOINT: "http://localhost:9000"
      S3_ACCESS_KEY: "minioadmin"
      S3_SECRET_KEY: "minioadmin"
      S3_BUCKET: "sw-media"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Node önce kurulmalı, pnpm cache ile birlikte
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          check-latest: true
          cache: "pnpm"

      # pnpm'i kur
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.19.0
          run_install: false

      # (Opsiyonel) pnpm doğrula
      - name: Verify pnpm installation
        run: |
          which pnpm
          pnpm --version

      # Turbo cache (opsiyonel ama hızlı)
      - name: Restore Turbo Cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-

      # Lock dosyasını kullanarak kurulum
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm -w lint

      - name: Typecheck
        run: pnpm -w typecheck

      - name: Prisma Generate (API)
        run: pnpm --filter @sg/api prisma:gen

      # NODE_ENV sadece build adımında production
      - name: Build
        env:
          NODE_ENV: "production"
        run: pnpm -w build

      - name: Save Turbo Cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
